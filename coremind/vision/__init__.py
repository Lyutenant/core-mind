"""Camera capture for the Node (Pi).

The Node grabs a still frame (cheap I/O); the Hub interprets it with a vision model
(the heavy work). See ``coremind/vision/base.py`` for the ``Camera`` interface.
"""
